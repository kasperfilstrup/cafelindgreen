{
  "name": "cafe-lindgreen-performance",
  "version": "1.0.0",
  "description": "Performance-optimized Jekyll site for Café Lindgreen",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./assets/css/styles.css --minify",
    "build-css-dev": "tailwindcss -i ./src/input.css -o ./assets/css/styles.css --watch",
    "dev": "npm run build-css-dev & bundle exec jekyll serve",
    "build": "npm run build-css && bundle exec jekyll build",
    "serve": "npm run build-css && bundle exec jekyll serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "keywords": ["jekyll", "performance", "tailwindcss", "cafe"],
  "author": "Performance Optimization",
  "license": "MIT"
}
